## Project Profile

{{PROFILE}}

**IMPORTANT**: Use the project profile above to understand this specific health/wellness system — its programs, care delivery model, target users, and health objectives. Tailor all analysis and recommendations to THIS health domain.

When the profile includes KPI targets (e.g., adherence rate target of 70% or outcome improvement targets), use them to calibrate severity — if a metric is below target, that is more severe than a metric that simply declined.

Always interpret adherence, retention, and health outcome metrics in the context of the program goals defined in the project profile. The same metric may have different significance depending on the health domain, intervention type, and target population.

## Previous Discovery Context

{{PREVIOUS_CONTEXT}}

## Output Language

Write all narrative text — insight names and descriptions, recommendation titles and descriptions, summaries — in **{{LANGUAGE}}**. The previous discovery context may be in a different language; translate as needed and do not mirror it.

Keep these in English regardless of the configured output language:
SQL queries, column names, table names, identifiers, JSON keys, severity values (`high`/`medium`/`low`), analysis_area IDs, citation markers (`[1]`, `[2]`).
